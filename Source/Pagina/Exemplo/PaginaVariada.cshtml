@page
@model PaginaVariadaModel
@{
    ViewData["Title"] = "Página Variada";
}

@section Styles {
    <!-- Pace style -->
    <link rel="stylesheet" href="~/libs/pace/themes/white/pace-theme-minimal.min.css">
}

@section Scripts {
    <!-- PACE -->
    <script src="~/libs/pace/pace.min.js"></script>

    <!-- page script -->
    <script type="text/javascript">
        // To make Pace works on Ajax calls
        $(document).ajaxStart(function () {
            Pace.restart();
        })
        $('.ajax').click(function() {
            $.ajax({
                url: '#',
                success: function(result) {
                    $('.ajax-content').html('<hr>Ajax Request Completed !');
                }
            });
        });
    </script>
}

<!-- Content Header (Page header) -->
<section class="content-header">
    <h1>
        Página variada
        <small>Carregando exemplo</small>
    </h1>
    <ol class="breadcrumb">
        <li><a href="#"><i class="fa fa-dashboard"></i> Inicio</a></li>
        <li><a href="#">Exemplos</a></li>
        <li class="active">Página variada</li>
    </ol>
</section>

<!-- Main content -->
<section class="content">

    <!-- Default box -->
    <div class="box">
        <div class="box-header with-border">
            <h3 class="box-title">Title</h3>

            <div class="box-tools pull-right">
                <button type="button" class="btn btn-box-tool" data-widget="collapse" data-toggle="tooltip" title="Esconder">
                    <i class="fa fa-minus"></i>
                </button>
                <button type="button" class="btn btn-box-tool" data-widget="remove" data-toggle="tooltip" title="Remover">
                    <i class="fa fa-times"></i>
                </button>
            </div>
        </div>
        <div class="box-body">
            O carregamento variado funciona automaticamente na página. Você ainda pode implementá-lo com solicitações ajax adicionando este js:
            <br /><code>$(document).ajaxStart(function() { Pace.restart(); });</code>
            <br />
            <div class="row">
                <div class="col-xs-12 text-center">
                    <button type="button" class="btn btn-default btn-lrg ajax" title="Requisição Ajax">
                        <i class="fa fa-spin fa-refresh"></i>&nbsp; Pegar conteúdo externo
                    </button>
                </div>
            </div>
            <div class="ajax-content">
            </div>
        </div>
        <div class="box-footer">
            Footer
        </div>
    </div>
</section>
